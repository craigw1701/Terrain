#version 400 core

in vec3 textureCoords;
out vec4 out_Color;

uniform samplerCube cubeMap;

void main(void)
{
    out_Color = mix(texture(cubeMap, textureCoords), vec4(0.988, 0.831, 0.25, 1), 0.5);
	
}